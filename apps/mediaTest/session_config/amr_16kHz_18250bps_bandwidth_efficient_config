# AMR-WB 18250 bps codec config file

codec_type = AMR_WB
bitrate = 18250              # bitrate in bps
sample_rate = 16000          # wideband encoder sampling rate
dtx_enable = 1
header_format = bandwidth-efficient  # format